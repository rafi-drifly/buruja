# Cloudflare Pages config for buruja.com
# Deploy from CLI:  npx wrangler pages deploy public --project-name=buruja
# (or connect the GitHub repo in the Cloudflare dashboard and set the output dir to `public`.)
name = "buruja"
pages_build_output_dir = "public"
compatibility_date = "2026-06-18"
